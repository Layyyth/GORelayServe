services:
  relay:
    image: laithaj/gorelayserve:latest
    container_name: go_relay
    restart: always
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      - RELAY_PORT=8080
